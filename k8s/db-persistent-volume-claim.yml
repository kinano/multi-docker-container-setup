apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-persistent-volume-claim
spec:
  accessModes:
    - ReadWriteOnce # Can be used by a single node
  resources:
    requests:
      storage: 1Gi
